{{# def.definitions }}
{{# def.errors }}
{{# def.setupKeyword }}
{{# def.$dataList }}


{{? ($schema || $isData) && it.opts.uniqueItems !== false }}
  {{? $isData }}
    var {{=$valid}};
    if ({{=$schemaValue}} === false || {{=$schemaValue}} === undefined)
      {{=$valid}} = true;
    else if (typeof {{=$schemaValue}} != 'boolean')
      {{=$valid}} = false;
    else {
  {{?}}

  var {{=$valid}} = true;
  if ({{=$dataList}}.length > 1) {
    var i = {{=$dataList}}.length, j;
    outer:
    for (;i--;) {
      for (j = i; j--;) {
        if (equal({{=$dataList}}[i], {{=$dataList}}[j])) {
          {{=$valid}} = false;
          break outer;
        }
      }
    }
  }

  {{? $isData }}  }  {{?}}

  if (!{{=$valid}}) {
    {{# def.error:'uniqueItems' }}
  } {{? $breakOnError }} else { {{?}}
{{??}}
  {{? $breakOnError }} if (true) { {{?}}
{{?}}
